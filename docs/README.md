# KMB API reference documents

This directory is reserved for local copies of the KMB ETA API specification and
data dictionary used during development. The reference PDFs are intentionally
ignored by Git; download them from the official DATA.GOV.HK dataset when needed.

The application source does not read these documents at runtime.
